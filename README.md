Simple application for downloading youtube videos

requires tkinter and pytube and pillow modules
use pip/pip3 install pytube in your terminal
use pip/pip3 install pillow in your terminal

Python 3.8
